import BtnSmall from "../../core/buttons/BtnSmall";

const LoginBtn = () => {
  return (
    <BtnSmall onClick={() => {}} disabled={false}>
      로그인
    </BtnSmall>
  );
};

export default LoginBtn;
